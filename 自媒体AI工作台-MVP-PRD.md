# AI 自媒体工作台 MVP PRD

**状态：** 产品设计初稿
**产品代号：** Creator OS
**形态：** Web 工作台优先；采集、媒体处理与发布使用独立 Worker。
**目标：** 让创作者或运营者在一个连续流程中完成内容情报、选题、创作、审核、发布与复盘。

## 1. 产品定位

> 面向创作者与内容运营者的 AI 内容操作系统：把分散的账号、素材、选题、稿件、AI 能力和发布任务组织为可持续运转的内容生产闭环。

产品不以“生成一篇文案”为主要价值，而以“持续稳定推进内容”为主要价值。AI 的角色是执行明确任务、产出可审核的内容资产，并把每次执行的来源、版本和结果留在工作台中。

### 不做什么

- 不做通用聊天机器人或泛知识库。
- 不在 MVP 追求全平台、全媒体、全自动矩阵发布。
- 不将采集实现、浏览器自动化或某一个开源项目暴露成核心业务模型。
- 不允许 Agent 绕过明确的审核状态直接发布。

## 2. 目标用户与首个闭环

### 用户

- 个人创作者：维护一个或少量账号，需要稳定地找选题、产出内容和安排发布。
- 内容运营者：维护品牌或账号矩阵，需要协同管理内容资产、审核和发布节奏。

### MVP 成功场景

用户以一个账号为单位，在工作台完成：

1. 从关键词、竞品内容或本地素材导入内容情报；
2. 将候选信息转化为带来源依据的选题卡；
3. 生成一个平台原生内容包（标题、正文、封面需求/提示词）；
4. 人工编辑并批准；
5. 加入发布日历，导出或交由已配置的发布连接器执行；
6. 保存发布记录，并在后续补充表现数据。

MVP 的首发内容类型为“图文内容包”，首发平台以小红书作为连接器验证目标；领域模型从第一天起保持多平台能力。

## 3. 核心流程

```text
内容情报 → 选题池 → 创作项目 → 审核批准 → 发布日历 → 表现记录
    ↑                                                        ↓
    └─────────────── 洞察与下一轮选题建议 ───────────────────┘
```

每一箭头都是可追踪的对象转换，不依赖聊天记录：

- 情报可被收藏、去重、标注和引用。
- 选题包含假设、目标受众、内容角度和证据。
- 创作项目从选题派生，持有多个可编辑版本。
- 发布必须经过显式批准，并产生不可变的发布记录。

## 4. 信息架构与 MVP 页面

### 4.1 今日工作台

默认首页，回答“今天该推进什么”。

- 待处理：待确认选题、待审核稿件、失败/需要人工处理的任务。
- 今日计划：发布日历中的当天内容与下一步动作。
- Agent 运行：进行中、成功、失败的任务及其产物入口。
- 快捷动作：导入素材、创建选题、创建内容项目。

### 4.2 内容情报

信息输入与沉淀入口。

- 来源：关键词采集、创作者订阅、单篇链接、浏览器扩展、本地文件。
- 内容卡：原始内容、作者/平台、互动指标、采集时间、媒体、评论和结构化摘要。
- 处理：去重、标签、收藏、归档、加入选题。
- 洞察：AI 从一组已选内容中输出共性、受众问题、内容结构和可验证机会；每个结论回链具体内容卡。

### 4.3 创作项目

从选题到可发布内容包的编辑中心。

- 左侧：引用证据与选题 Brief。
- 中间：可编辑内容（标题、正文、标签、CTA）。
- 右侧：版本、平台适配、封面/配图需求、Agent 操作与审核状态。
- Agent 仅生成候选版本；编辑器中由人决定采纳、合并或重写。

### 4.4 发布日历

按日期管理已批准内容，而非替代内容编辑。

- 未排期、待发布、发布中、成功、失败五种状态。
- 展示平台、账号、内容类型、发布时间和责任人。
- MVP 支持导出发布包；已配置连接器时支持单条发布或定时任务。

## 5. 领域对象与状态

| 对象 | 核心字段 | 关键状态/规则 |
| --- | --- | --- |
| Workspace | 名称、成员、默认时区 | 数据与账号的根边界 |
| Channel | 平台、账号显示名、连接器配置引用 | 已连接 / 失效 / 需人工登录 |
| EvidenceItem | 来源、原文、媒体、指标、采集时间、标签 | 候选 / 收藏 / 归档 |
| Topic | 受众、洞察、角度、证据引用、优先级 | 草稿 / 已确认 / 已归档 |
| ContentProject | 目标平台、Brief、主题、关联选题 | 草稿 / 待审核 / 已批准 / 已退回 |
| ContentVersion | 标题、正文、媒体需求、生成来源 | 不可变版本；可标记为当前版本 |
| Asset | 文件、转录/OCR、许可/来源信息、标签 | 原始资产与可复用资产分开 |
| PublishJob | 平台、账号、计划时间、内容版本、执行日志 | 待排期 / 待发布 / 执行中 / 成功 / 失败 |
| MetricSnapshot | 时间窗口、内容、平台指标、原始数据 | 只追加，不覆盖历史快照 |
| AgentRun | 输入、工具调用、引用、输出、错误 | 可重试；不可伪造为人工批准 |

## 6. Agent 与 Skills 设计

MVP 不建立“万能 Agent”。使用一个任务编排器，执行五种边界清晰的 Skill：

1. **情报分析**：从给定内容卡提取结构、论点、评论问题、受众信号。
2. **选题生成**：基于已选证据提出候选选题，并注明引用和不确定性。
3. **平台创作**：按 Brief 生成小红书图文候选版本、标题组与封面需求。
4. **内容审校**：检查内容是否缺证据、重复、偏离账号语气或缺失必要字段。
5. **发布准备**：将已批准版本转换为指定连接器需要的发布包；无发布授权不得执行。

每一次运行必须保存：输入对象 ID、所用 Skill/Prompt 版本、模型、工具调用、引用证据、输出对象 ID、失败原因和时间。

## 7. 技术架构

```text
React/Next.js Web
        │
        ▼
Business API ─────────────── PostgreSQL
        │                         │
        ├── Job Queue / Redis      └── Object Storage（原始媒体与文件）
        │
        ├── Collector Worker ── MediaCrawler / Spider_XHS 适配器
        ├── Media Worker ───── 转录、OCR、图片/视频生成
        ├── Publisher Worker ─ social-auto-upload 适配器
        └── Agent Orchestrator ─ Skills、模型与任务日志
```

### 技术选择

- 前端：Next.js、React、TypeScript、Tiptap（内容编辑）。
- API/Worker：Python + FastAPI；采集与发布生态均可直接复用 Python 项目。
- 数据：PostgreSQL + pgvector（后期检索）；S3 兼容对象存储；Redis 队列。
- 异步任务：初期 Redis Queue/Celery 或 Dramatiq；Agent 任务通过统一 `AgentRun` 记录状态。
- 桌面端：不纳入 MVP。未来有强本地登录态、文件管理或后台运行需要时，用 Tauri 封装 Web 前端。

## 8. 外部项目复用边界

| 项目 | 复用角色 | 不能成为核心依赖的部分 |
| --- | --- | --- |
| Beav | 产品流程、Electron/React 组织与浏览器采集体验参考 | 不直接耦合其本地工作区和 IPC 设计 |
| MediaCrawler | 多平台采集 Worker | 其配置与存储模型不外泄至领域层 |
| Spider_XHS | 小红书专用采集/发布 Adapter | 平台签名、Cookie 与内部 API 不进入业务模型 |
| Viral Writer Skill | 写作洞见、标题与配图产出框架 | 不作为不可替换的唯一 Prompt |
| self-media-content-workflow | Brief、策略、平台文案、复盘的 Skill 分层 | 不将其文件流程当数据库状态机 |
| social-auto-upload | 发布 Worker/CLI 调用 | 不复用其 Web UI 作为主工作台 |
| DSH Desktop | 插件、任务、权限、执行记录的设计参考 | 不在 MVP 嵌入完整桌面 Agent runtime |

所有第三方能力均通过 `Connector` 或 `Skill` 契约调用。若某个项目失效，只替换对应适配器，已有情报、稿件、发布记录不受影响。

## 9. 开发阶段

### Phase 1：可编辑内容闭环

- 工作区、账号频道、素材导入、内容情报卡。
- 选题池与证据引用。
- 创作项目、AI 候选稿、版本管理与人工审核。
- 发布日历与发布包导出。

**验收：** 用户能从一条导入内容生成一份可审核、可导出的图文内容包，所有引用与版本可回溯。

### Phase 2：真实连接器与任务系统

- 接入小红书采集 Worker 与浏览器登录态管理。
- AgentRun、队列、重试、执行日志与人工介入。
- 小红书发布连接器或定时发布。

**验收：** 失败任务可定位、重试或转人工；发布记录能关联到具体稿件版本和执行日志。

### Phase 3：多平台与复盘

- 抖音/公众号等平台适配。
- 数据快照、内容与选题复盘、下一轮选题建议。
- 可安装 Skill 与连接器注册表。

**验收：** 同一个选题可生成多平台原生内容包，并能从数据回溯到选题和创作版本。

## 10. 非目标与产品护栏

- 自动发布只接受已批准的内容版本与明确的账号/时间配置。
- 采集、发布和 Agent 任务均为可观察的后台任务，不以“聊天消息已发送”代替完成状态。
- 平台连接器必须支持健康状态与人工登录恢复，不把 Cookie/令牌直接写入业务数据。
- 优先保证“单账号、单平台、单内容类型”的完整可用闭环，再扩展。

## 11. 下一次产品决策

开始工程实现前，仅需确认：

1. MVP 是否以“小红书图文内容包”作为首条可验收闭环；
2. 是否接受 Web 优先、Worker 独立、暂不做桌面端；
3. 是否先完成“导出发布包 + 人工发布”，把自动发布放入 Phase 2。
